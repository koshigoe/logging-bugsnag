require 'spec_helper'

describe Logging::Bugsnag do
  it 'has a version number' do
    expect(Logging::Bugsnag::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
